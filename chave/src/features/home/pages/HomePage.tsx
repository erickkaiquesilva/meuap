export function HomePage() {
  return (
    <div style={{ padding: '64px 24px', textAlign: 'center' }}>
      <h1 style={{ fontSize: '32px', fontWeight: 800, color: 'var(--primary-500)', marginBottom: '12px' }}>
        chave<span style={{ color: 'var(--secondary-500)' }}>.</span>
      </h1>
      <p style={{ color: 'var(--neutral-500)' }}>Home — em desenvolvimento (STORY-02)</p>
    </div>
  )
}
